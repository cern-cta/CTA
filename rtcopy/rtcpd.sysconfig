# Options to pass to the rtcpd
#RTCPD_OPTIONS=

# Should we run with another limit on the core file size other than the default?
DAEMON_COREFILE_LIMIT=unlimited

# Security
if [ -r /etc/sysconfig/castor_security ]; then
        . /etc/sysconfig/castor_security CENTRAL
        export SECURE_CASTOR=1
        export CSEC_TRACE=1
        export CSEC_TRACEFILE=/var/log/castor/rtcpd.csec.log
fi
