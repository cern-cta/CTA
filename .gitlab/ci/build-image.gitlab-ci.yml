# SPDX-FileCopyrightText: 2025 CERN
# SPDX-License-Identifier: GPL-3.0-or-later

.build-cta-image:
  stage: build:image
  needs:
    - job: build-cta-rpms
      optional: true # We don't need this in every pipeline type
    - job: modify-project-json
      optional: true
  rules:
    - if: $PIPELINE_TYPE == "SYSTEM_TEST_ONLY"
      when: never
    # TODO: handle the different pipelines types such as regression tests
    - when: on_success
  image:
    name: $IMAGE_DOCKER_IMAGE_BUILDER
    entrypoint: [""]
  variables:
    CONTAINER_STORAGE: "/builds/container-storage"
    TARGET_NAME: # Should be overwritten by one of the child jobs
    CTA_BUILD_ID: ${CI_PIPELINE_ID}git${CI_COMMIT_SHORT_SHA}
    GIT_SUBMODULE_STRATEGY: "none" # Don't need submodules for these jobs
    DOCKERFILE: ci/docker/${PLATFORM}/prod.Dockerfile
    IMAGE_NAME: ctageneric/${TARGET_NAME} # ctageneric is important because that's the private repo.
    IMAGE_REF: ${CI_REGISTRY}/cta/${IMAGE_NAME}:${CTA_BUILD_ID}
    # Important that this matches the artifact path from the build job
    RPM_CONTEXT: ${CI_PROJECT_DIR}/build_rpm/RPM/RPMS/x86_64
    EXTRA_BUILD_ARGS: ""
  before_script:
    # Configure storage
    - mkdir -p ${CONTAINER_STORAGE}
    - mkdir -p ~/.config/containers/
    - |
      cat > ~/.config/containers/storage.conf << EOF
      [storage]
       driver = "overlay"
       runroot = "${CONTAINER_STORAGE}/run/containers/storage"
       graphroot = "${CONTAINER_STORAGE}/.local/share/containers/storage"
       rootless_storage_path = "${CONTAINER_STORAGE}/.local/share/containers/storage"
       [storage.options]
       pull_options = {enable_partial_images = "true", use_hard_links = "false", ostree_repos=""}
       [storage.options.overlay]
      EOF
    - cp ~/.config/containers/storage.conf /etc/containers/storage.conf || true
    - echo "$DOCKER_LOGIN_PASSWORD" | buildah login -u "$DOCKER_LOGIN_USERNAME" --password-stdin $CI_REGISTRY
  script:
    - echo "Building target ${TARGET_NAME}"
    # TODO: dnf caching
    # TODO: internal repo and oracle flag
    # ensure build context is correct
    - cd "$(dirname "${DOCKERFILE}")"
    - DOCKERFILE_NAME="$(basename "${DOCKERFILE}")"
    - BUILDAH_ARGS=(
        --target "${TARGET_NAME}"
        --file "${DOCKERFILE_NAME}"
        --tag "${IMAGE_REF}"
        --build-context rpm_context="${RPM_CONTEXT}"
        --build-arg USE_INTERNAL_REPOS=1
        --build-arg USE_ORACLE_CATALOGUE=1
      )
    # TODO: check if caching makes sense. Not many layers to cache though so it may just hurt speed and caching /var/cache/dnf may be enough
    - if [ -n "$EXTRA_BUILD_ARGS" ]; then BUILDAH_ARGS+=($EXTRA_BUILD_ARGS); fi
    - buildah bud "${BUILDAH_ARGS[@]}" .
    # TODO: add a sanity check here if oracle is in the image and if the registry is public
    # if so, fail the job so that we prevent accidentally pushing oracle rpms somewhere public
    # TODO: image size metric?
    - echo "Pushing to ${IMAGE_REF}"
    - buildah push "${IMAGE_REF}"

###############################################
# Image Targets
###############################################

build-image-taped:
  extends:
    - .build-cta-image
  variables:
    TARGET_NAME: "cta-taped"

build-image-rmcd:
  extends:
    - .build-cta-image
  variables:
    TARGET_NAME: "cta-rmcd"

build-image-maintd:
  extends:
    - .build-cta-image
  variables:
    TARGET_NAME: "cta-maintd"

# TODO: conditionally switch the variable to -grpc
build-image-frontend:
  extends:
    - .build-cta-image
  variables:
    TARGET_NAME: "cta-frontend-xrd"

build-image-tools:
  extends:
    - .build-cta-image
  variables:
    TARGET_NAME: "cta-tools-xrd"

###############################################
# Publishing
###############################################

# TODO: add job that uses buildah to retag images. This way we can publish them
# We'll need the usual checks
