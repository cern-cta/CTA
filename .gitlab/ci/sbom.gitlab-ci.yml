# SPDX-FileCopyrightText: 2025 CERN
# SPDX-License-Identifier: GPL-3.0-or-later

spec:
  inputs:
    enable-sbom-generation:
      type: boolean
---

.sbom-rules:
  rules:
    - if: '$PIPELINE_TYPE != "DEFAULT" && $PIPELINE_TYPE != "REGR_AGAINST_CTA_BRANCH"'
      when: never
    - if: '"$[[ inputs.enable-sbom-generation ]]" == "true"'
      when: on_success
    - when: never

enrich-sbom-cern-info:
  extends: .sbom-rules
  stage: sbom
  needs:
    - job: build-cta-images
      artifacts: true
      parallel:
        matrix:
          - TARGET_NAME: [cta-taped, cta-rmcd, cta-maintd, cta-frontend, cta-tools]
  image: registry.cern.ch/code-scanners/sbom-helper:latest
  variables:
    SBOM_TARGETS: "cta-taped cta-rmcd cta-maintd cta-frontend cta-tools"
  script:
    - mkdir -p sboms
    - |
      export SERVICE_ELEMENT="CTA for Physics"
      export UNIT="IT-SD-TAB"
      export CONTACT="$GITLAB_USER_EMAIL"
      export ANNOTATOR="$GITLAB_USER_NAME"
      export URL="$CI_PROJECT_URL"

      for target in $SBOM_TARGETS; do
        input="sboms/raw/${target}.cdx.json"
        output="sboms/${target}.cdx.json"

        if [[ ! -f "$input" ]]; then
          echo "Missing raw SBOM for ${target}: ${input}" >&2
          exit 1
        fi

        export APP_NAME="$target"
        python /app/src/sbom-helper/main.py --env -i "$input"
        cp "sboms-output/${input}" "$output"
      done
  artifacts:
    expire_in: 30 days
    paths:
      - sboms/*.cdx.json

score-sbom:
  extends: .sbom-rules
  stage: sbom
  needs:
    - job: enrich-sbom-cern-info
      artifacts: true
  image: $IMAGE_SBOMQS
  variables:
    SBOM_MINIMUM_SCORE: "8.0"
    SBOM_TARGETS: "cta-taped cta-rmcd cta-maintd cta-frontend cta-tools"
  script:
    - |
      failed=0

      for target in $SBOM_TARGETS; do
        sbom="sboms/${target}.cdx.json"
        if report="$(/app/sbomqs score "$sbom" 2>&1)"; then
          status=0
        else
          status=$?
        fi
        echo "$report"

        if [[ $status -ne 0 ]]; then
          echo "Failed to score ${sbom}" >&2
          failed=1
          continue
        fi

        score="$(printf '%s\n' "$report" | sed -nE 's/.*[Ss]core:[[:space:]]*([0-9]+([.][0-9]+)?).*/\1/p' | head -n 1)"
        if [[ -z "$score" ]]; then
          echo "Could not extract the overall score for ${sbom}" >&2
          failed=1
          continue
        fi

        if ! awk -v score="$score" -v minimum="$SBOM_MINIMUM_SCORE" 'BEGIN { exit !(score >= minimum) }'; then
          echo "${sbom} scored ${score}, below the required ${SBOM_MINIMUM_SCORE}" >&2
          failed=1
        fi
      done

      exit "$failed"
  artifacts:
    expire_in: 30 days
    paths:
      - sboms/*.cdx.json
    reports:
      cyclonedx:
        - sboms/*.cdx.json
