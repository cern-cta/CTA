# Options to pass to the rfiod
#RFIOD_OPTIONS=

# Should we run with another limit on the core file size than the default?
DAEMON_COREFILE_LIMIT=unlimited

# Security
if [ -f /etc/sysconfig/castor_security ]; then
        . /etc/sysconfig/castor_security DISK
        export SECURE_CASTOR=1
        export CSEC_TRACE=1
        export CSEC_TRACEFILE=/usr/spool/rfio/rfio_trace
fi
