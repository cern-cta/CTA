apiVersion: v1
kind: ConfigMap
metadata:
  name: scripts-client
data:
  client.sh: |
    {{ .Files.Get "scripts/client.sh" | nindent 4 }}
  client-grpc.sh: |
    {{ .Files.Get "scripts/client-grpc.sh" | nindent 4 }}
