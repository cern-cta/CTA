apiVersion: v2
name: client
description: Sets up an EOS client (with extra CTA RPMs) for a CTA + EOS instance.
type: application
version: 0.2.0
appVersion: "5.11.10.1"

dependencies:
- name: common
  version: 0.1.0
  repository: "file://../common"
