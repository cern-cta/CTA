apiVersion: v1
kind: Service
metadata:
  name: auth-token-issuer
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: auth-token-issuer
  clusterIP: None
  ports:
    - port: 80
      targetPort: 8000
