apiVersion: v2
name: ci-init
version: 0.2.0
description: An initilaization chart for the cta project when running in a ci context. It configures persistent volumes and prepares that resources for the CTA pods. Must be launched BEFORE the rest
type: application

dependencies:
  - name: kdc
    version: 0.1.0

appVersion: "1.16.0"
