apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    config: versioning
  name: versionlock-conf
  namespace: {{.Release.Namespace}}
data:
  versionlock.list: |
    {{-  required "A valid .Values.versionlockFile needs to be provided" .Values.versionlockFile | nindent 4 }}
