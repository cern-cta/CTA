{{- $namespace := .Release.Namespace -}}
{{- $name := include "ctaeos.name" . -}}
# We use a post-install hook to wait for eos to be ready instead of a readiness probe
# The reason being is that the startup script needs access to the service
# But the service will only be reachable when the pod is ready
# As such, the pod will be ready immediately and we use this to wait for it to be done initializing
apiVersion: v1
kind: Pod
metadata:
  name: {{ $name }}-post-install
  namespace: {{ $namespace | quote }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  restartPolicy: Never
  containers:
  - name: wait-for-eos
    image: busybox
    command:
      - /bin/sh
      - -c
      - |
        echo -n "Waiting for EOS to be initialised"
        while [ ! -f /eos-status/EOS_READY ]; do
          echo -n "."
          sleep 1
        done
        echo ""
        echo "EOS initialised"
    volumeMounts:
      - name: eos-status
        mountPath: /eos-status
  volumes:
    - name: eos-status
      persistentVolumeClaim:
        claimName: eos-status-pvc
