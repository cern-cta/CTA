apiVersion: v2
name: ctafrontend
description: A frontend helm chart for Kubernetes in CTA project. Sets up a frontend pod and a service with defined configuration in values.yaml file.
type: application
version: 0.1.0
appVersion: "1.16.0"
