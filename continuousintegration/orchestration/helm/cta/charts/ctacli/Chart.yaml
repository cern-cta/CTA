apiVersion: v2
name: ctacli
description: A CTA admin cli helm chart for Kubernetes in CTA project. Sets up a cli pod and a service with defined configuration in values.yaml file.
type: application
version: 0.2.0
appVersion: "1.16.0"
