apiVersion: v1
kind: ConfigMap
metadata:
  name: scripts-maintenance
data:
  maintenance.sh: |
    {{ .Files.Get "scripts/maintenance.sh" | nindent 4 }}
  pre_run.sh: {{ .Values.preRun.script | toYaml | nindent 4 }}
  post_run.sh: {{ .Values.postRun.script | toYaml | nindent 4 }}
