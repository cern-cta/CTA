# SPDX-FileCopyrightText: 2025 CERN
# SPDX-License-Identifier: GPL-3.0-or-later
apiVersion: v2
name: scheduler
version: 0.1.0
description: A chart that sets up a configmap for the scheduler. Due to the nature of the scheduler, there are no pods to spawn; only a job that sets up/resets the scheduler. Requires the init chart to have been installed.
type: application
appVersion: "5.11.10.1"

dependencies:
- name: common
  version: 0.1.1
  repository: "file://../common"
