apiVersion: v1
kind: ConfigMap
metadata:
  name: etc-cta
data:
  cta-catalogue.conf: |-
    {{ include "init.catalogue.url" . }}
  cta-objectstore-tools.conf: |-
    ObjectStore BackendPath {{ include "init.scheduler.url" . }}
