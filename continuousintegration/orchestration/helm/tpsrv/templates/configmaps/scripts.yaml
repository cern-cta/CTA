apiVersion: v1
kind: ConfigMap
metadata:
  name: scripts-tpsrv
data:
  rmcd.sh: |
    {{ .Files.Get "scripts/rmcd.sh" | nindent 4 }}
  taped.sh: |
    {{ .Files.Get "scripts/taped.sh" | nindent 4 }}
  pre_run.sh: {{ .Values.preRun.script | toYaml | nindent 4 }}
  post_run.sh: {{ .Values.postRun.script | toYaml | nindent 4 }}
