replicaCount: 1

conf:
  cta:
    endpoint: cta-frontend:10955
  eos:
    instance: eos-dev-mgm
    requester:
      user: adm
      group: powerusers

pod:
  extraSpec:

# Additional Extra environment variables
extraEnv:

extraVolumes:
  volumes:
  volumeMounts:
