replicaCount: 1

conf:
  cta:
    endpoint: cta-frontend:10955

pod:
  extraSpec:
    terminationGracePeriodSeconds: 1

# Additional Extra environment variables
extraEnv:

extraVolumes:
  volumes:
  volumeMounts:
