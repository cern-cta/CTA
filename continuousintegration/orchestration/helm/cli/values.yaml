component: cli
partOf: cta

replicaCount: 1

conf:
  cta:
    endpoint: cta-frontend:10955
  eos:
    instance: eos-dev-mgm
    requester:
      user: adm
      group: powerusers

# Additional Extra environment variables
extraEnv: {}
