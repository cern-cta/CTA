apiVersion: v1
kind: Pod
metadata:
  name: cta-compile-pod
  labels:
    k8s-app: cta-compile-pod
spec:
  restartPolicy: Never
  containers:
  - name: cta-compile-pod
    image: gitlab-registry.cern.ch/linuxsupport/alma9-base
    stdin: true
    command: ["sleep", "infinity"]
    volumeMounts:
    - mountPath: /shared
      name: shared

  volumes:
  - name: shared
    hostPath:
      path: /opt/cta

  imagePullSecrets:
  - name: ctaregsecret
