dcache:
  plugins:
    cta:
      enabled: true
      endpoint: "cta-frontend-grpc:10956"
      user: dcacheuser
      group: dcacheusers
      instance: dcache

  pools:
    - a

  hsm:
    enabled: true

kafka:
  enabled: false
