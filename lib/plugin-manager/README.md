# Plugin Manager

This header-only library provides the common interface and dynamic loading support used to register and instantiate CTA plugins.

Consumers should link against the `ctapluginmanager` CMake target and include its headers using the `plugin-manager/` prefix.
