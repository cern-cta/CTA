# CTA Protobuf

This directory builds CTA's generated protobuf types and the conversions between protobuf messages and CTA domain types.
Public headers are in `include/`, implementations in `src/`, unit tests in `test/`, and upstream sources in `external/`.
