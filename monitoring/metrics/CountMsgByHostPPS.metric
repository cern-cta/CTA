# Count the number of message sent by each host, on PPS

<metric>
  name: CountMsgByHostC2PPS
  window: 60
  conditions: INSTANCE == "c2pps"
  groupbykeys: HOSTNAME
  data: CounterHz(COUNT)
  handle_unordered: time_threshold
  nbins: 3
</metric>
