.PS

copy "sequence.pic";

# Put a vertical space at the top of the document
step();
step();

# Define the objects
object(G,"gateway");
placeholder_object(Fake_1);
placeholder_object(Fake_2);
object(B,"bridge");
#placeholder_object(Fake_2);
object(C,"rtcpd child");

step();

# Message sequences

active(B);

active(C);
message(C,B,"flushed to tape");
message(B,C,"ack");
inactive(C);

message(B,G,"connect");
active(G);
message(B,G,"FileMigrationReportList for files 1 and 2");
message(G,B,"NotificationAcknowledge");
message(B,G,"close connection");
inactive(G);

step();

inactive(B);

complete(C);
complete(B);
complete(G);

.PE
