.PS

copy "sequence.pic";

# Define the objects
object(RC,"rtcpcld");
placeholder_object(C);
object(V,"vdqm");

placeholder_object(Fake_1);
placeholder_object(Fake_2);

object(R,"rtcpd");

step();

# Message sequences
active(RC);
message(RC,V,"connect");
active(V);
message(RC,V,"volume request");
message(V,RC,"ack");
message(V,RC,"volReqID");
message(RC,V,"ack");
message(RC,V,"disconnect");
inactive(V);

step();

active(V);
message(V,R,"connect");
active(R);

message(V,R,"ClientInfo - job");
message(R,V,"RcpJobReply");
message(R,V,"disconnect");
inactive(V);

message(R,RC,"connect");
message(RC,R,"request ReqInfo");
message(R,RC,"ack");
message(R,RC,"ReqInfo");
message(RC,R,"ack");

create_message(RC,C,"migrator");
active(C);
message(C,R,"ClientInfo - new callback");
message(R,C,"ack");
message(C,R,"disconnect");

step();

message(R,C,"connect");

# Start of getRequestList
message(C,R,"volume info");
message(R,C,"ack");

message(C,R,"file 1");
message(R,C,"ack");

message(C,R,"Offer_MoreWork - Joker");
message(R,C,"ack");

message(C,R,"NoMore_Req");
message(R,C,"ack");
# End of getRequestList

step();
message(R,C,"connect for tape IO");
message(R,C,"connect for disk IO");
message(R,C,"connect for disk IO");
message(R,C,"connect for disk IO");
step();

message(R,C,"Request_MoreWork (1)");
# Acknowledge comes after getRequestList()

# Start of getRequestList
message(C,R,"file 2");
message(R,C,"ack");

message(C,R,"Offer_MoreWork - Joker");
message(R,C,"ack");

message(C,R,"NoMore_Req");
message(R,C,"ack");

# End of getRequestList

# Better late than never
# Acknowledge of tape IO's request for more work
message(C,R,"ack of Request_MoreWork (1)");

step();

message(R,C,"Request_MoreWork (2)");
message(C,R,"ack");

message(C,R,"End_of_List");
message(R,C,"ack");

# Better late than never
# Acknowledge of tape IO's request for more work
message(C,R,"ack of Request_MoreWork (2)");

step();

message(R,C,"positioned for file 1 (tape req)");
message(C,R,"ack");
message(R,C,"positioned for file 1 (file req)");
message(C,R,"ack");

message(R,C,"Transfert file 1");
message(C,R,"ack");

step();


message(R,C,"positioned for file 2 (tape req)");
message(C,R,"ack");
message(R,C,"positioned for file 2 (file req)");
message(C,R,"ack");

message(R,C,"Tranfert file 2");
message(C,R,"ack");

step();

message(R,C,"End_of_Req");
message(C,R,"ack");
inactive(R);

inactive(C);
inactive(RC);

complete(RC);
complete(C);
complete(V);
complete(R);

.PE

