{{- /*
SPDX-FileCopyrightText: 2025 CERN
SPDX-License-Identifier: GPL-3.0-or-later
*/ -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-scripts
data:
  kdc.sh: |
    {{ .Files.Get "scripts/kdc.sh" | nindent 4 }}
  generate_secret_files.sh: |
    {{ .Files.Get "scripts/generate_secret_files.sh" | nindent 4 }}
  generate_secrets.sh: |
    {{ .Files.Get "scripts/generate_secrets.sh" | nindent 4 }}
  generate_jwks.py: |
    {{ .Files.Get "scripts/generate_jwks.py" | nindent 4 }}
  generate_jwt.py: |
    {{ .Files.Get "scripts/generate_jwt.py" | nindent 4 }}
  create_k8s_secrets.py: |
    {{ .Files.Get "scripts/create_k8s_secrets.py" | nindent 4 }}
