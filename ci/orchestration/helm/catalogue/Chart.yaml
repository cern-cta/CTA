# SPDX-FileCopyrightText: 2025 CERN
# SPDX-License-Identifier: GPL-3.0-or-later

apiVersion: v2
name: catalogue
version: 0.1.0
description: A chart that sets up a configmap for the catalogue, a job to initialise/reset the catalogue and (if the backend allows) spawns a catalogue database. Must be launched after the init chart.
type: application
appVersion: "6.0.0.0"

dependencies:
  - name: common
    version: 0.1.1
    repository: "file://../common"
