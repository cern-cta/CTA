# Manifests

Directory containing plain Kubernetes files. Ideally we really on helm charts everywhere, so this directory should be kept as minimal as possible.
Right now it contains some dCache specific files that should be migrated to something more robust at a later point in time.
