# Unused

These scripts were present before the System Test migration from Bash to Python, but they were never called in the test.
If someone ever decides we need to include these again in the system tests, they should be rewritten from scratch in Python.
For now, these files are left here as reference.
