# Arguments for the Cupvdaemon
#   -c <path>      configuration file
#   -t <nbthreads
#
# Options to pass to the Cupvdaemon
#CUPVDAEMON_OPTIONS=

# Should we run with another limit on the core file size other than the default?
DAEMON_COREFILE_LIMIT=unlimited

# Security
if [ -f /etc/sysconfig/castor_security ]; then
        . /etc/sysconfig/castor_security CENTRAL
        export SECURE_CASTOR=1
        export CSEC_TRACE=1
        export CSEC_TRACEFILE=/usr/spool/upv/cupv_trace
fi
