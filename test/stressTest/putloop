#!/usr/bin/python

import os
from putUtils import *
from stressUtils import *

# all the preparation is done in baseputloop, we only have to execute the command
counter = 0
while True:
    createDirIfNeeded(baseCastorDir, counter)
    fileName = getCastorDir(baseCastorDir, counter) + os.sep + inputFileBaseName + "_" + getHost() + "_" + buildUuid()
    command = baseCmd + pathToCastorCommands + "rfcp " + inputFile + " " + fileName + " >> " + logFile + " 2>> " + logFile
    os.system(command)
    counter = counter + 1
