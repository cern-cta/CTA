.PS

copy "sequence.pic";

# Put a vertical space at the top of the document
step();
step();

# Define the objects
object(W,"worker");
object(C,"rtcpd child");

step();

# Message sequences

active(W);
active(C);

message(C,W,"waiting");
message(W,C,"ack");

step();

inactive(W);
inactive(C);

complete(W);
complete(C);

.PE

