.PS

copy "sequence.pic";

# Put a vertical space at the top of the document
step();
step();

# Define the objects
object(B,"bridge");
placeholder_object(Fake_2);
placeholder_object(Fake_3);
object(C,"rtcpd child");

step();

# Message sequences

active(C);

message(C,B,"positioned for file 1 (tape request)");
active(B);
message(B,C,"ack");
inactive(B);

message(C,B,"positioned for file 1 (file request)");
active(B);
message(B,C,"ack");
inactive(B);

message(C,B,"file 1 written but not flushed");
active(B);
message(B,C,"ack");
inactive(B);

step();

message(C,B,"positioned for file 2 (tape request)");
active(B);
message(B,C,"ack");
inactive(B);

message(C,B,"positioned for file 2 (file request)");
active(B);
message(B,C,"ack");
inactive(B);

message(C,B,"file 2 written but not flushed");
active(B);
message(B,C,"ack");
inactive(B);

step();

inactive(C);

complete(C);
complete(B);

.PE
